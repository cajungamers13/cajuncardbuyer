{
  "name": "tcg-trading-post",
  "version": "1.0.0",
  "description": "Tiered buy-offer calculator for TCG collections — Capacitor wrapper for iOS/Android around the static web app.",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-web": "node scripts/sync-web.js",
    "sync": "npm run sync-web && cap sync",
    "open:ios": "npm run sync-web && cap open ios",
    "open:android": "npm run sync-web && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2",
    "typescript": "^7.0.2"
  }
}
